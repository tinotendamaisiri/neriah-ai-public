__pycache__/
*.pyc
.env
.env.*
.env.local
backend/.env
neriah-website/.env.local
.venv/
venv/
*.egg-info/
.DS_Store
credentials.json
*.pem
*.key

# React Native / Expo
node_modules/
.expo/
.vercel

# Claude Code per-user settings (machine-specific, leaks paths + project IDs)
.claude/

# AI model files (downloaded at runtime, not bundled)
*.task
app/mobile/assets/models/

# EAS build artifacts pulled down for local install (not committed)
app/mobile/build-*.apk

# Native build outputs (regenerated by gradle/xcodebuild)
app/mobile/android/build/
app/mobile/android/app/build/
app/mobile/android/.gradle/
app/mobile/android/.kotlin/
app/mobile/ios/build/
app/mobile/ios/Pods/
app/mobile/ios/Podfile.lock
app/mobile/dist/

# patch-package failure dumps
patch-package-error.json*

# Test + tooling caches
.pytest_cache/
*.tsbuildinfo

# R REPL history (no R in the project — guard against accidental re-creation)
.Rhistory

# Editor / OS / one-off backup files
*.bak
*.swp
*.swo

# Stray root-level lockfile from accidental npm runs
/package-lock.json
CLAUDE.md
